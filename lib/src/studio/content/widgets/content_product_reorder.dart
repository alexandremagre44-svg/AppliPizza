// lib/src/studio/content/widgets/content_product_reorder.dart
// Widget for reordering products within categories (for future implementation)

import 'package:flutter/material.dart';

class ContentProductReorder extends StatelessWidget {
  const ContentProductReorder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Product reorder widget - To be implemented'),
    );
  }
}
