/// POS Routes - route definitions for the POS module
/// 
/// This file defines all routes related to the POS system with proper
/// module-based protection.
library;

/// POS route constants
class PosRoutes {
  /// Main POS screen route
  static const String pos = '/pos';
  
  /// POS history screen route (future)
  static const String posHistory = '/pos/history';
  
  /// POS settings screen route (future)
  static const String posSettings = '/pos/settings';
}
