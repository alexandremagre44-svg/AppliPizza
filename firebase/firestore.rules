rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMP RULES - open for index deploy only
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
